{
  "name": "@estudeme/cli",
  "version": "0.0.0",
  "description": "EstudeMe CLI — vault validation, trail progress, cards export",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "estudeme": "./dist/index.js"
  },
  "files": ["dist", "templates", "skills"],
  "scripts": {
    "prebuild": "node scripts/copy-skills.mjs",
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo skills"
  },
  "dependencies": {
    "@estudeme/core": "*",
    "commander": "^12.0.0",
    "kleur": "^4.1.5",
    "cli-table3": "^0.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "gray-matter": "^4.0.3",
    "tsup": "^8.0.0",
    "vitest": "^2.0.0"
  }
}
